{
  "name": "@ruigomeseu/ffcli",
  "version": "0.1.2",
  "description": "Fireflies.ai CLI — query meeting data from the command line",
  "type": "module",
  "bin": {
    "ffcli": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run tests/unit",
    "test:e2e": "vitest run tests/e2e",
    "test:all": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "bun build --compile src/index.ts --outfile ffcli",
    "build:npm": "bun build src/index.ts --outfile dist/index.js --target node --banner \"#!/usr/bin/env node\"",
    "prepublishOnly": "bun run build:npm"
  },
  "keywords": ["fireflies", "meetings", "transcripts", "cli"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ruigomeseu/ffcli.git"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^4.0.18"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "graphql": "^16.12.0",
    "graphql-request": "^7.4.0"
  }
}
